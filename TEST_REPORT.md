# Integration Test Report

Automated checks cover health, API loading/pagination, invalid form rejection, and valid application submission.

Run: `npm install` then `npm test`.

Manual checks: loading state, failure message, Retry button, search/domain filtering, client validation, and successful form submission are implemented in the frontend.
